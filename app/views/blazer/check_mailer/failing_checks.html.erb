<ul>
  <% @checks.each do |check| %>
    <li><%= link_to check.query.name, blazer.query_url(check.query_id) %> <%= check.state %></li>
  <% end %>
</ul>
<p><%= link_to "Manage checks", checks_url %></p>
